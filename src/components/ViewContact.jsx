import { useState } from "react"

export default function ViewContact() {
    const [contact, setContact] = useState([])

    return (
        <section>
            <h2></h2>
        </section>
    )
}